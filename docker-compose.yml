version: '3.8'
services:
  dashboard:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./cache:/app/cache
      - ./logs:/app/logs
    environment:
      - TZ=America/Phoenix
    restart: unless-stopped
